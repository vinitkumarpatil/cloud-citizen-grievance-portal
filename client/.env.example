# Leave empty for local dev (Vite proxies /api to the backend on :4000).
# For a cloud deployment, set this to the deployed backend origin, e.g.
# VITE_API_URL=https://grievance-api.example.com
VITE_API_URL=
